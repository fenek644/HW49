<%= form_with(model: po_image, local: true) do |form| %>
  <% if po_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(po_image.errors.count, "error") %> prohibited this po_image from being saved:</h2>

      <ul>
      <% po_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
